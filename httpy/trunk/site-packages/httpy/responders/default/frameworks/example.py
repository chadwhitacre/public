from httpy import Request


class MyRequest(Request):

    _views = ( 'raw'
             , 'screen'
             , 'print'
              )

    view = 'screen'

    def __init__(self, request):
        """Takes an httpy.Request.
        """
        Request.__init__(self, request)
        if request.path.startswith('/print/'):
            self.view = 'printer'
            self.path = '/'.join(parts)


class Framework:

    def wrap_request(self, request, responder=None):
        """
        """
        if responder is None:
            return MyRequest(request)
        else:
            return request


    def unwrap_response(self, responder, request, response):
        name = 'format_%s' % request.view
        method = getattr(self, name)
        return method(response)


    def format_print(self, response):
        """
        """

    def format_error(self, response):
        """
        """

    def format_content(self, response):
        """
        """

    def format_homepage(self, response):
        """
        """

