from httpy._zope.interface import implements
from httpy.responders.default.interfaces import IFramework


class Framework:

    implements(IFramework)

    def wrap_request(self, responder, request):
        return request

    def unwrap_response(self, responder, request, response):
        return response
