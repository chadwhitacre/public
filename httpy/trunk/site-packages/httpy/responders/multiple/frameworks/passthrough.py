from httpy._zope.interface import implements
from httpy.responders.multiple.interfaces import IFramework


class Framework:

    implements(IFramework)

    def wrap_request(self, request, responder=None):
        return request

    def unwrap_response(self, response, request, responder):
        return response
