"""This is a collection of utilities for httpy and httpy applications.
"""
from httpy.utils.mode import mode
from httpy.utils.parse import *
from httpy.utils.reloader import reloader
from httpy.utils.daemon import daemonize


def isiter(foo):
    return hasattr(foo, '__iter__') and hasattr(foo, 'next')
