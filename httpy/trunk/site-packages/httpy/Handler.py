from SocketServer import StreamRequestHandler

class Handler(StreamRequestHandler):

    def handle(self):
        self.request = self.server.Request(self.rfile)
        self.response = self.server.Response(self.wfile)
        print "HANDLED"