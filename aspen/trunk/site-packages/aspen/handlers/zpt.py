from aspen import zpt # avoid namespace collision

class Handler:

    def __init__(self, website):
        self.cache = zpt.httpyTemplateCache()

    def handle(self, **context):
        template = self.cache[context['request'].fspath]
        return template.render(context)
