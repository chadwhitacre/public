from httpy import Response


class Handler:

    def __init__(self, website):
        pass

    def handle(self, **context):
        __file__ = context['__file__'] = context['request'].fspath
        try:
            exec open(__file__) in context
        except SystemExit:
            pass
        return context['response']