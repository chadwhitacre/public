"""Rules distributed with Aspen.

Rules take a file object and a predicate and return a boolean.

"""
import fnmatch as _fnmatch
import os

def rule_fnmatch(fp, predicate):
    return _fnmatch.fnmatchcase(fp.filename, predicate)

